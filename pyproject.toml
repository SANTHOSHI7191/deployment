[project]
name = "deployment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SANTHOSHI7191", email = "santhoshijanapala91@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
deployment = "deployment:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
